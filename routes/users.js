const express = require('express')
const router = express.Router()

// SHOW USER
router.get('/', (req, res) => {
  res.send('user')  
})

// CREATE NEW USER

// DELTE USER

// UPDATE USER PROFILE

module.exports = router
